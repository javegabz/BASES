(()=>{

    enum AudioLevel {
        min=1,
        medium,
        max=10,
        ultramax 
    };

    //let currentAudio = AudioLevel.ultramax;
    let currentAudio = AudioLevel.ultramax;
    console.log(currentAudio);
    console.log(AudioLevel);

})()

